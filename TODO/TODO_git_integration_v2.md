# Plan: GitHub Actions CI + Fixture Hosting for integration_test

## Context

The integration test harness for prolfquapp is complete and passing locally (403 assertions, 0 failures). Now we need:

1. **CI automation** — run integration tests automatically when any upstream package changes
2. **Fixture serving** — the 45 MB of generated fixture data needs to be available in CI without committing it to git

### Current state
- `integration_test/` is a standalone git repo (not yet on GitHub)
- Fixtures are gitignored (generated by `make fixtures` which runs `create_test_fixtures.R`)
- Fixture generation downloads ~91 MB from Zenodo (PTM data) + ~135 MB from prolfquadata R package (IonStar data)
- Reference SEs are 2.8 MB total (small enough to commit)
- The upstream packages live in **two GitHub orgs**: `fgcz` (prolfqua) and `prolfqua` (prolfquapp, prolfquappPTMreaders)

### Problem with prolfquadata dependency
The prolfquadata R package (227 MB installed) lives on private GitLab (`gitlab.bfabric.org`), not on CRAN/Bioconductor. CI can't reach this GitLab. Solution: upload the package tarball to Zenodo as-is and install from there. No code changes needed — `system.file(..., package = "prolfquadata")` calls keep working.

Files sourced from prolfquadata by `create_test_fixtures.R`:
- `quantdata/annotation_Ionstar2018_PXD003881.xlsx` (8 KB)
- `quantdata/MAXQuant_IonStar2018_PXD003881.zip` (106 MB)
- `quantdata/MSFragger_IonStar2018_PXD003881.zip` (29 MB)
- `fastaDBs/uniprot-proteome_UP000005640_reviewed_yes.fasta.gz` (7.3 MB)

### Fixture size breakdown
| File | Size |
|------|------|
| `fixtures/fragpipe_ionstar/MSstats.csv` | 18 MB |
| `fixtures/fp_tmt_total/p1/psm.tsv` | 11 MB |
| `fixtures/fp_tmt_total/p2/psm.tsv` | 10 MB |
| `fixtures/maxquant_ionstar/txt/peptides.txt` | 6 MB |
| Everything else (configs, FASTAs, datasets, singlesite) | < 1 MB |
| **Total fixtures/** | **~45 MB** |
| `fixtures/reference/*.rds` | 2.8 MB |

## Plan

### Step 1: Commit reference SEs to git

The 4 reference `.rds` files (2.8 MB total) are small enough to track in git. Move them from gitignored `fixtures/reference/` to a tracked `reference/` directory at the integration_test root.

**Files to modify:**
- `integration_test/.gitignore` — keep `fixtures/` ignored, but add explicit `reference/` (tracked)
- `integration_test/tests/testthat/test-dea-regression.R` — update `REFERENCE_DIR` to point to `<project_root>/reference/` instead of `fixtures/../reference/`
- `integration_test/Makefile` — update `save-references` target output path and `clean-references` target
- Move existing `fixtures/reference/*.rds` → `reference/*.rds`

### Step 2: Upload prolfquadata to Zenodo (as-is)

**Goal:** Make prolfquadata installable in CI without access to private GitLab.

**Approach:** Build the package tarball and upload it to Zenodo unchanged. No disassembly — `system.file()` calls in `create_test_fixtures.R` keep working exactly as they do now.

**Steps (manual, user action required):**
1. Build tarball: `R CMD build <path-to-prolfquadata-source>` → produces `prolfquadata_0.1.0.tar.gz`
   - If source isn't available, create tarball from installed: package up `/Users/wolski/Library/R/4.5-arm64/prolfquadata/` as a tarball
2. Upload `prolfquadata_0.1.0.tar.gz` to Zenodo (new deposit or add to existing record https://zenodo.org/records/15879865)
3. Record the download URL and MD5

### Step 3: Install prolfquadata from Zenodo in CI and fixture generation

**No changes to `R/create_test_fixtures.R`** — all `system.file(..., package = "prolfquadata")` calls stay as-is.

**Changes to `integration_test/Makefile`:**
- Add a `install-prolfquadata` target that downloads the tarball from Zenodo (cached in `.cache/`) and runs `R CMD INSTALL`
- Make `fixtures` depend on `install-prolfquadata`

**Alternatively**, handle this in the GitHub Actions workflow directly:
```yaml
- name: Cache prolfquadata tarball
  uses: actions/cache@v4
  with:
    path: .cache/prolfquadata_0.1.0.tar.gz
    key: prolfquadata-v0.1.0

- name: Install prolfquadata from Zenodo
  run: |
    if [ ! -f .cache/prolfquadata_0.1.0.tar.gz ]; then
      mkdir -p .cache
      curl -L -o .cache/prolfquadata_0.1.0.tar.gz "$PROLFQUADATA_ZENODO_URL"
    fi
    R CMD INSTALL .cache/prolfquadata_0.1.0.tar.gz
```

### Step 4: Fixture hosting — generate in CI (no pre-built artifacts)

**Decision: generate fixtures fresh in CI** rather than hosting pre-built artifacts.

Rationale:
- `make fixtures` takes ~3 min and is fully deterministic
- Zenodo is a stable, permanent archive — URLs won't break
- prolfquadata installed from Zenodo tarball (no private GitLab access needed)
- The `.cache/` directory can be cached between CI runs with `actions/cache`

**CI caching strategy:**
- Cache `.cache/` (PTM Zenodo ZIP ~91 MB + prolfquadata tarball ~140 MB)
- Fixture generation with warm cache: ~1 min (no downloads, just subsetting)

### Step 5: GitHub Actions workflow for integration_test

Create `.github/workflows/integration-tests.yml` in the `integration_test` repo.

**Triggers:**
- `workflow_dispatch` — manual trigger (always useful)
- `schedule` — weekly (`cron: '0 6 * * 1'`, Monday 6am UTC)
- `repository_dispatch` — receives cross-repo trigger events

**Workflow steps:**
```yaml
name: Integration Tests

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * 1'  # Weekly Monday 6am UTC
  repository_dispatch:
    types: [upstream-change]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: 'release'

      - name: Cache downloads (prolfquadata + Zenodo PTM data)
        uses: actions/cache@v4
        with:
          path: .cache/
          key: zenodo-fixtures-v2  # bump when Zenodo URLs change

      - name: Install prolfquadata from Zenodo
        run: |
          if [ ! -f .cache/prolfquadata_0.1.0.tar.gz ]; then
            mkdir -p .cache
            curl -L -o .cache/prolfquadata_0.1.0.tar.gz "$PROLFQUADATA_URL"
          fi
          R CMD INSTALL .cache/prolfquadata_0.1.0.tar.gz
        env:
          PROLFQUADATA_URL: https://zenodo.org/records/XXXXXXX/files/prolfquadata_0.1.0.tar.gz  # TBD

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: |
            fgcz/prolfqua
            prolfqua/prolfquapp
            prolfqua/prolfquappPTMreaders
            local::.

      - name: Generate fixtures
        run: make fixtures

      - name: Run integration tests
        run: make test

      - name: Run regression tests
        run: make test-dea-regression
```

**Note:** `setup-r-dependencies@v2` reads a DESCRIPTION file. We'll need a minimal `DESCRIPTION` in `integration_test/` listing the ecosystem packages as dependencies. This is the standard r-lib/actions pattern for non-package projects.

**File to create:** `integration_test/DESCRIPTION` (minimal, not a real package — just for dependency resolution):
```
Type: Project
Description: Integration tests for the prolfqua ecosystem.
Depends:
    prolfqua,
    prolfquapp,
    prolfquappPTMreaders,
    testthat (>= 3.0.0),
    SummarizedExperiment,
    seqinr,
    readxl,
    readr,
    yaml,
    tidyr
Remotes:
    fgcz/prolfqua,
    prolfqua/prolfquapp,
    prolfqua/prolfquappPTMreaders
```

**Note:** `prolfquadata` is NOT listed here — it's installed separately from the Zenodo tarball before `setup-r-dependencies` runs. This avoids `r-lib/actions` trying to resolve it from CRAN/Bioconductor/Remotes.

### Step 6: Cross-repo trigger workflows

Add a small "sender" workflow to each upstream repo that fires a `repository_dispatch` event to the integration_test repo when code is pushed.

#### 4a. In `fgcz/prolfqua` — `.github/workflows/trigger-integration.yml`
```yaml
name: Trigger Integration Tests

on:
  push:
    branches: [main, master, Modelling2R6]
    paths: ['R/**', 'DESCRIPTION', 'NAMESPACE']

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.INTEGRATION_PAT }}
          repository: <owner>/integration_test  # TBD: where this repo lives
          event-type: upstream-change
          client-payload: '{"repo": "prolfqua", "ref": "${{ github.sha }}"}'
```

#### 4b. In `prolfqua/prolfquapp` — same workflow, different branch filter
#### 4c. In `prolfqua/prolfquappPTMreaders` — same workflow

**PAT token setup:**
- Create a GitHub Personal Access Token (classic) with `repo` scope
- Add it as a secret named `INTEGRATION_PAT` in each upstream repo's Settings → Secrets
- The PAT must belong to a user/bot with write access to the integration_test repo

### Step 7: Push integration_test to GitHub

**Decision needed:** Which org/account should host the `integration_test` repo?

Options:
- `fgcz/prolfqua-integration-tests` — in the same org as prolfqua core
- `prolfqua/integration-tests` — in the same org as prolfquapp
- Personal account — simplest for PAT management

The repo name in Step 4's `repository` field depends on this choice.

### Step 8: Makefile updates for CI compatibility

Minor tweaks to ensure `make` targets work in CI:

- `make fixtures` already works (downloads from Zenodo only, no R data packages needed)
- `make test` already works (runs testthat)
- `make save-references` — only needed locally to update baselines, not in CI
- Add `make test-regression` alias if not present (currently `test-dea-regression`)

## Files to create/modify

| File | Action | Description |
|------|--------|-------------|
| `integration_test/reference/` | create dir | Move reference SEs here (tracked in git) |
| `integration_test/.gitignore` | edit | Remove `fixtures/reference` exception if any |
| `integration_test/tests/testthat/test-dea-regression.R` | edit | Update REFERENCE_DIR path |
| `integration_test/Makefile` | edit | Update reference paths, CI-compatible targets |
| `integration_test/R/create_test_fixtures.R` | no change | `system.file()` calls stay as-is |
| `integration_test/DESCRIPTION` | create | Minimal project DESCRIPTION for r-lib/actions (no prolfquadata) |
| `integration_test/.github/workflows/integration-tests.yml` | create | Main CI workflow |
| `prolfqua/.github/workflows/trigger-integration.yml` | create | Cross-repo trigger |
| `prolfquapp/.github/workflows/trigger-integration.yml` | create | Cross-repo trigger |
| `prolfquappPTMreaders/.github/workflows/trigger-integration.yml` | create | Cross-repo trigger |

## Manual steps (user action required)

1. **Build prolfquadata tarball**: `R CMD build <prolfquadata-source>` → `prolfquadata_0.1.0.tar.gz`
2. **Upload tarball to Zenodo** — upload as-is, record download URL
3. **Update Zenodo URL** in `.github/workflows/integration-tests.yml` `PROLFQUADATA_URL` env var
4. **Create PAT** and add as `INTEGRATION_PAT` secret in upstream repos
5. **Push** integration_test repo to GitHub

## Verification

1. Push `integration_test` to GitHub → Actions tab shows the workflow
2. Manually trigger via `workflow_dispatch` → workflow installs deps, generates fixtures, runs tests, all pass
3. Push a commit to `prolfquapp` → `trigger-integration.yml` fires → integration tests run automatically
4. Break something in prolfqua (e.g. rename an R6 method) → push → integration tests fail → notification
